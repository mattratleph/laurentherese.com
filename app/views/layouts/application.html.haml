!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title laurentherese.com
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body
    %p.notice= notice
    %p.alert= alert

    %header
      %nav
        %ul
          - Category.all.each do |category|
            %li= link_to category.name, posts_path(category: category.name)
          %li= link_to 'Meet Lauren', '/about'
          %li= link_to 'Contact Lauren', '/contact'
      %h1= link_to 'Lauren Therese Photography', root_path

    %section.content
      = yield

    %footer
      %small
        &copy; 2013 -
        = Time.now.year
        All Rights Reserved
      - if user_signed_in?
        %p= link_to 'Sign out', destroy_user_session_path, :method => :delete
